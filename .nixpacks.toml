[phases.setup]
apt_pkgs = ["wget", "ca-certificates"]

[phases.install]
commands = [
  "pip install -r requirements.txt",
  "playwright install chromium"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"